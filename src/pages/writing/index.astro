---
// All section content lives on `/` now. This route exists so that
// deep links (e.g. /writing shared in an email) resolve to the
// right section on the single-page home.
const base = import.meta.env.BASE_URL || "";
return Astro.redirect(base + "/#writing");
---
